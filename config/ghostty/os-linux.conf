# =============================================================================
# Ghostty config — Linux/WSL-specific overrides
# =============================================================================
# Symlinked to ~/.config/ghostty/os.conf by install.sh on Linux.
# Anything Linux-only goes here so the common config stays portable.
# =============================================================================

# (No Linux-specific overrides currently. Add as needed.)
